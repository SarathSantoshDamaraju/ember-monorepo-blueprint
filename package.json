{
  "name": "ember-addon-generator",
  "version": "0.0.1",
  "private": true,
  "description": "Ember addon blueprint to create a monorepo ",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspace sample-addon start"
  },
  "devDependencies": {
    "ember-cli": "~3.7.1",
    "lerna": "^3.22.1"
  },
  "engines": {
    "node": ">=10.* || >=12.*",
    "yarn": "^1.17.3"
  }
}
